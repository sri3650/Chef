<%= node.default[:ivin_application][:nginx_log_path] %>/*.log {
  daily
  missingok
  dateext
  create 660 root app
  sharedscripts
  postrotate
    if [ -f /opt/nginx/logs/nginx.pid ]; then
      sudo -u app /etc/init.d/nginx reload > /dev/null
    fi
  endscript
}